This is a dataset of images of just basketballs. There are only four images in the training set and two in the validation set,
so training can be done quickly. Also using mask_rcnn_balloon.h5 weights file to train. 
